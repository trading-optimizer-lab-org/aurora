name: SP500 Strategy Catalog Overnight 360 (retired entrypoint)

on:
  workflow_call:
    inputs:
      protected_commit_sha:
        required: true
        type: string

permissions:
  actions: read
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    timeout-minutes: 1
    steps:
      - run: |
          echo "This legacy launcher is inactive; use the signed catalog request controller."
          exit 1
