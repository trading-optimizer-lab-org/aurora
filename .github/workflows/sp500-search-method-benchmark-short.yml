name: SP500 Search Method Benchmark Short

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  expanded:
    uses: ./.github/workflows/sp500-search-method-benchmark-expanded.yml
    permissions:
      actions: read
      contents: read
