# QuantForge dashboard Deployment skeleton.
# Documentation only; values must be overridden by Helm / kustomize.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantforge-dashboard
  labels:
    app: quantforge
    component: dashboard
spec:
  replicas: 2
  selector:
    matchLabels:
      app: quantforge
      component: dashboard
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: quantforge
        component: dashboard
    spec:
      containers:
        - name: dashboard
          image: quantforge/dashboard:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8501
          envFrom:
            - configMapRef:
                name: quantforge-config
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "2Gi"
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
