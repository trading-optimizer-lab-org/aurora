"""QuantForge multi-agent system.

P1.B: contains the ``auditor`` package — specialized reviewer agents that
emit structured, NON-binding review reports. Reports are inputs to the
validation pipeline; the LLM never decides trades or approves promotion.

See :mod:`quantforge.agents.auditor` for the public API.
"""
from __future__ import annotations
